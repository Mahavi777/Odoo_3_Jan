# Copy this file to .env and fill values before running the backend
# MongoDB connection string (if not provided, a local DB will be used)
MONGO_URI=mongodb://127.0.0.1:27017/odooxgcet_dev

# JWT secret for authentication tokens
JWT_SECRET=your_jwt_secret_here

# Server port
PORT=5000
